{
    "domain_name": "{{DOMAIN_NAME}}",
    "snaplinks_cleanup_required": {{SNAPLINKS_CLEANUP_REQUIRED}},
    "shards": [{{#INDEX_MORAY_SHARDS}}
        {
            "host": "{{host}}"
        }{{^last}},{{/last}}{{/INDEX_MORAY_SHARDS}}
    ]
}
