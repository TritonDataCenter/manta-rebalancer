{
    "domain_name": "{{DOMAIN_NAME}}",
    "sapi_url": "{{SAPI_URL}}"
    "shards": [{{#INDEX_MORAY_SHARDS}}
        {
            "host": "{{host}}"
        }{{^last}},{{/last}}{{/INDEX_MORAY_SHARDS}}
    ]
}
